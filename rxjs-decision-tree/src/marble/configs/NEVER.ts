// src/marble/configs/NEVER.ts
import type { FirstOrderDiagramConfig } from '../marble.types'
export const neverConfig: FirstOrderDiagramConfig = {
	operatorName: 'NEVER',
	totalTime: 8,
	source: {
		values: [],
	},
	result: {},
}
